{
  "name": "is-this-ci",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "is-this-ci": "bin/is-this-ci.js"
  },
  "scripts": {
    "is-this-ci": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json && npm run sbom",
    "prepare": "npm run build",
    "sbom": "cyclonedx-npm --output-file dist/sbom.json --output-format json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "chart.js": "^4.4.3",
    "tsx": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@cyclonedx/cyclonedx-npm": "^1.13.0",
    "typescript": "^5.4.5",
    "vitest": "^4.0.16"
  },
  "publishConfig": {
  "@YOUR-USERNAME:registry": "https://npm.pkg.github.com"
  }
}
